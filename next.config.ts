import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["images.unsplash.com", "yt3.googleusercontent.com"], // Agrega el dominio de la imagen aquí
  },
};

export default nextConfig;
